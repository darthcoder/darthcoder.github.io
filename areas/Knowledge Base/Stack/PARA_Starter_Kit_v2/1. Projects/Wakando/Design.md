The project needs these key components:

1. **Command parser** - Interprets syntax like `#tags`, `@time`, etc.
2. **Date/time handler** - Processes natural language dates
3. **macOS integration** - Bridges to Reminders and Calendar
4. **Error handling** - Validates input and provides feedback
5. **Configuration storage** - Saves preferences
6. **CLI entrypoint** - Processes arguments
7. **System tray app** - Background process for hotkeys (future)
8. **UI** - Command bar interface (future)

The critical path for MVP:

1. Command parser
2. macOS Reminders API integration
3. Basic CLI functionality

Would you like me to start with the command parser implementation?